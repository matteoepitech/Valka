function <i32> puts(<char*> string);
function <i32> putchar(<i32> char);

function <void> put_nbr(<i32> n)
{
    var <i32> tmp = 0;

    if (n >= 10) {
        put_nbr(n / 10);
    }
    tmp = n % 10;
    putchar(tmp + 48);
}

function <void> long_func()
{
    for (var <i32> y = 0; y < 1000; y = y + 1) {
        for (var <i32> x = 0; x < 1000; x = x + 1) {
            if (x % 2 == 0) {
                putchar(88);
            }
            if (x % 2 != 0) {
                putchar(111);
            }
        }
        putchar(10);
    }
}

function <i32> main(<i32> argc)
{
    var <i32> old_result = 0;
    var <i32> tmp = 0;
    var <i32> result = 1;

    puts("Result of the fibonacci algorithm:");
    for (var <i32> i = 0; i < argc; i = i + 1) {
        tmp = result;
        result = result + old_result;
        old_result = tmp;
        put_nbr(result);
        putchar(10);
    }
    var <i32> my_var = 50;
    for (var <i32> j = 0; j < argc; j = j + 1) { }
    while (my_var > 0) {
        my_var = my_var - 1;
        putchar(48);
    }
    return 0;
}
