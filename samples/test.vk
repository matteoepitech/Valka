function <i32> puts(<char*> string);
function <i32> putchar(<i32> char);

function <void> put_nbr(<i32> n)
{
    var <i32> tmp = 0;

    if (n >= 10) {
        put_nbr(n / 10);
    }
    tmp = n % 10;
    putchar(tmp + 48);
}

function <i32> main()
{
    return 0;
}
