function <i32> puts(<char*> string);

function <i32> aa()
{
    return 11;
}

function <i32> main()
{
    var <i32> is_logged = 5 + 6;

    if (aa() == is_logged) {
        puts("You are now logged!");
        return 0;
    }
    puts("You are not logged!");
    return 1;
}
